# Shared Workspace

Use this directory for cross-layer artifacts when needed, such as:

- API request/response DTO types
- Shared enums and constants
- Validation schemas reused by both client and server
